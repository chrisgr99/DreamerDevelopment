build/
dist/
plugin.dylib
plugin.so
plugin.dll
*.vcvplugin
.DS_Store

# Video: the renders and the recordings they are cut from. All of it is rebuildable from
# docs/images/make-video.py and demo/narration.txt, and none of it belongs in a repository —
# the finished film alone is forty megabytes.
demo/clips/
*.mp4
*.mov
*.m4a
*.wav
*.aiff

# The browsable manuals, rebuilt from the markdown by docs/render.py.
docs/*.html
