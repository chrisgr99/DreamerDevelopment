# Ports whose own citation names a bound while the range field is blank.
# 393 ports across 31 plugins.

== 4msCompany
   SHEV in10 — SHEVCore.cc:297-300 read isPatched on this jack and, when it is patched, take getInputOrLast from it in place of the shaped envelope as that side's VCA control, scaled and clamped into the SSI2162 gain at SHEVCore.cc:303-316
   SHEV in11 — SHEVCore.cc:297-300 read isPatched on this jack and, when it is patched, take getInputOrLast from it in place of the shaped envelope as that side's VCA control, scaled and clamped into the SSI2162 gain at SHEVCore.cc:303-316

== ALMFree
   OAx2 in0 — plugin.dylib OAx2::process 0x11eec-0x11efc reads one voltage, multiplies it by param 1 (configured -2 to +2 at 0x11b04) and adds param 0 (configured -10 to +10 at 0x11a9c); 0x11f2c-0x11f6c clamps the result to -12V and 12V

== AS
   DelayPlusStereoFx in12 — DelayPlusStereo.cpp:228 reads channel one of this input only (getVoltage with no channel), divides it by ten and clamps it to 0..1 before the trigger, so it takes 10V to clear the buffer
   DelayPlusStereoFx in13 — DelayPlusStereo.cpp:308 reads channel one of this input only (getVoltage with no channel), divides it by ten and clamps it to 0..1 before the trigger, so it takes 10V to clear the buffer
   SEQ16 in3 — SEQ16.cpp:184 reads channel one of this input only (getVoltage with no channel), adds it to the STEPS knob and rounds the sum to a whole number of steps clamped to 1..16

== AmalgamatedHarmonics
   Bombe in4 — Bombe.cpp:182 read with getVoltage() alone, with no channel loop and clamps the Y knob plus a tenth of the voltage between 0 and 1
   Generative in3 — Generative.cpp:234 read with getVoltage() alone, with no channel loop and clamps the NOISE MIX knob plus the voltage between 0 and 1
   Generative in5 — Generative.cpp:207 read with getVoltage() alone, with no channel loop and clamps the CLOCK knob plus the voltage between -2 and 6 octaves of tempo
   Generative in6 — Generative.cpp:270 read with getVoltage() alone, with no channel loop and clamps the PROB knob plus a tenth of the voltage between 0 and 1
   Progress in5 — Progress.cpp:231 read with getVoltage() alone, with no channel loop and rounds the STEPS knob plus the voltage, clamped between 1 and 8, one step per whole volt
   Progress2 in5 — Progress2.cpp:183 read with getVoltage() alone, with no channel loop and rounds the STEPS knob plus the voltage, clamped between 1 and 8, one step per whole volt
   Ruckus in2 — Ruckus.cpp:149 reads getVoltage(i) for all sixteen channels of this input and clamps each division knob plus 6.4 times that channel between 0 and 64
   Ruckus in3 — Ruckus.cpp:150 reads getVoltage(i) for all sixteen channels of this input and clamps each probability knob plus a tenth of that channel between 0 and 1
   Ruckus in4 — Ruckus.cpp:151 reads getVoltage(i) for all sixteen channels of this input and clamps each shift knob plus 12.8 times that channel between -64 and 64

== Animatek
   UzzX in0 — UZZ.cpp:717 the expander offset is rounded and added to the STEPS knob, then clamped to 1..16
   UzzX in1 — UZZ.cpp:720 the offset is added to the START knob, then clamped
   UzzX in2 — UZZ.cpp:852 the offset is added to the DIR parameter, then clamped to the ten orders
   UzzX in4 — UZZ.cpp:648 the offset is added to the ratio index, then clamped
   UzzX in7 — UZZ.cpp:933 the offset is rounded and added to the accumulator amount, then clamped to 0..24

== AudibleInstruments
   Blinds in4 — Blinds.cpp:62 divides this input by 5, multiplies by the CV attenuverter and adds the gain knob, and Blinds.cpp:63 clamps the total to -2..2, the span a fifth of ±10V covers
   Blinds in5 — Blinds.cpp:62 divides this input by 5, multiplies by the CV attenuverter and adds the gain knob, and Blinds.cpp:63 clamps the total to -2..2, the span a fifth of ±10V covers
   Blinds in6 — Blinds.cpp:62 divides this input by 5, multiplies by the CV attenuverter and adds the gain knob, and Blinds.cpp:63 clamps the total to -2..2, the span a fifth of ±10V covers
   Blinds in7 — Blinds.cpp:62 divides this input by 5, multiplies by the CV attenuverter and adds the gain knob, and Blinds.cpp:63 clamps the total to -2..2, the span a fifth of ±10V covers
   Braids in3 — Braids.cpp:94 divides this input by 5, multiplies by the MOD knob and adds the TIMBRE knob, and Braids.cpp:96 clamps the total to 0..1
   Braids in4 — Braids.cpp:95 divides this input by 5 and adds the COLOR knob, and Braids.cpp:97 clamps the total to 0..1
   Clouds in2 — Clouds.cpp:165 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Clouds in3 — Clouds.cpp:166 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Clouds in5 — Clouds.cpp:176 divides this input by 5 and adds it to whichever parameter the BLEND button has selected, and that parameter is then clamped to 0..1
   Clouds in8 — Clouds.cpp:168 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Clouds in9 — Clouds.cpp:169 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Elements in6 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in7 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in8 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in9 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in10 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in11 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in12 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in13 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in14 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and the BIND macro at Elements.cpp:209 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total at 0
   Elements in15 — Elements.cpp:157 takes getChannels() on the V/OCT jack to set the voice count and Elements.cpp:220 reads this input with getPolyVoltage(c), scales it by a fifth times its attenuverter and clamps the knob-plus-input total to 0..2
   Marbles in0 — Marbles.cpp:466 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Marbles in1 — Marbles.cpp:495 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Marbles in4 — Marbles.cpp:468 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Marbles in5 — Marbles.cpp:444 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Marbles in6 — Marbles.cpp:497 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Marbles in7 — Marbles.cpp:493 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Rings in0 — Rings.cpp:155 divides this input by 5, multiplies it by 3.3 times its squared attenuverter and adds the BRIGHTNESS knob; the total is clamped at 0
   Rings in1 — Rings.cpp:168 reads this input with getNormalVoltage(1.0), divides by 5 and scales it by 48 times 3.3 times the quartic FM knob, clamped to plus or minus four octaves
   Rings in2 — Rings.cpp:156 divides this input by 5, multiplies it by 3.3 times its squared attenuverter and adds the DAMPING knob; the total is clamped at 0
   Rings in4 — Rings.cpp:157 divides this input by 5, multiplies it by 3.3 times its squared attenuverter and adds the POSITION knob; the total is clamped at 0
   Stages in0 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Stages in1 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Stages in2 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Stages in3 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Stages in4 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Stages in5 — Stages.cpp:225 divides this input by 8, adds the stage's slider and clamps the total to 0..1, the span an eighth of 0 to 8V covers
   Tides in0 — Tides.cpp:115 divides this input by 5, adds the knob and clamps the total to -1..1, the span a fifth of ±5V covers
   Tides in1 — Tides.cpp:116 divides this input by 5, adds the knob and clamps the total to -1..1, the span a fifth of ±5V covers
   Tides in2 — Tides.cpp:117 divides this input by 5, adds the knob and clamps the total to -1..1, the span a fifth of ±5V covers
   Tides2 in0 — Tides2.cpp:232 divides this input by 10, multiplies by the cubed attenuverter and clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   Tides2 in1 — Tides2.cpp:205 multiplies this input by its attenuverter and by 12 into a frequency modulation in semitones, clamped to plus or minus eight octaves
   Tides2 in3 — Tides2.cpp:234 divides this input by 10, multiplies by the cubed attenuverter and clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   Tides2 in4 — Tides2.cpp:233 divides this input by 10, multiplies by the cubed attenuverter and clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   Tides2 in5 — Tides2.cpp:235 divides this input by 10, multiplies by the cubed attenuverter and clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   Warps in0 — Warps.cpp:78 divides this input by 5, adds the knob and clamps the total to 0..1; in the internal oscillator algorithms Warps.cpp:96 instead reads it with getNormalVoltage(2.0) as a pitch at twelve semitones per volt
   Warps in1 — Warps.cpp:79 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Warps in2 — Warps.cpp:80 divides this input by 5, adds an eighth of the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers
   Warps in3 — Warps.cpp:91 divides this input by 5, adds the knob and clamps the total to 0..1, the span a fifth of 0 to 5V covers

== Autinn
   Amp in1 — Amp.cpp:67 asks this input isPolyphonic() and Amp.cpp:75 reads channel c with getPolyVoltage; Amp.cpp:80 clamps the knob-plus-input sum to 0 to 2, so a negative voltage reduces the gain to a floor of silence
   Bass in3 — Bass.cpp:383 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 1
   Bass in4 — Bass.cpp:382 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 1.25
   Bass in5 — Bass.cpp:386 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0.2 to 2 seconds
   Bass in6 — Bass.cpp:385 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 1
   Big in1 — Myria.cpp:166 reads this input with getVoltage, multiplies by 2 and Myria.cpp:167 truncates and clamps the knob-plus-input sum to 0 to 19
   Big in2 — Myria.cpp:169 reads this input with getVoltage, multiplies by 0.1 and clamps the knob-plus-input sum to 0 to 1
   Boomerang in2 — Boomerang.cpp:62 reads this input with getVoltage and clamps the knob-plus-input sum to 0.001 to 2, so a negative voltage reduces the gain to that floor
   Coil in3 — Coil.cpp:304 reads this input with getVoltage, multiplies it by 0.3 and Coil.cpp:305 clamps the knob-plus-input sum to 0 to 3
   Coil in4 — Coil.cpp:307 reads this input with getVoltage, multiplies it by 0.12 and Coil.cpp:308 clamps the knob-plus-input sum to 0 to 1.2
   Coil in5 — Coil.cpp:310 reads this input with getVoltage, multiplies it by 0.1 and Coil.cpp:311 clamps the knob-plus-input sum to 0 to 1
   Coil in6 — Coil.cpp:325 reads this input with getVoltage, multiplies it by 0.1 and Coil.cpp:326 clamps the knob-plus-input sum to 0.05 to 0.95
   Coil in7 — Coil.cpp:313 reads this input with getVoltage, multiplies it by 15 and Coil.cpp:314 clamps the knob-plus-input sum to 10 to 160 milliseconds
   Coil in9 — Coil.cpp:323 reads this input with getVoltage, multiplies it by 0.4 and Coil.cpp:324 clamps the knob-plus-input sum to 0.05 to 4
   Deadband in1 — Deadband.cpp:83 takes getChannels() on this input and Deadband.cpp:91 reads channel c with getPolyVoltage when it carries more than one; Deadband.cpp:94 clamps the knob-plus-input total to 0 to 5
   Deadband in2 — Deadband.cpp:84 takes getChannels() on this input and Deadband.cpp:92 reads channel c with getPolyVoltage when it carries more than one; Deadband.cpp:95 subtracts it from the knob and clamps to 0 to 1, so a negative voltage raises the gap
   Digi in1 — Digi.cpp:77 reads this input with getVoltage, which is channel 0 only, and clamps the knob-plus-input total to 0 to 1
   Excavi in2 — Excavi.cpp:501 reads channel c of this input with getPolyVoltage, multiplies it by 0.1 and Excavi.cpp:503 clamps the knob-plus-input sum to 0 to 1
   Excavi in3 — Excavi.cpp:506 reads channel c of this input with getPolyVoltage, multiplies it by 0.1 and Excavi.cpp:508 clamps the knob-plus-input sum to 0 to 1
   Excavi in5 — Excavi.cpp:470 reads channel c of this input with getPolyVoltage and Excavi.cpp:471 multiplies it by 10 and clamps the knob-plus-input sum to 0 to 60 years
   Excavi in7 — Excavi.cpp:520 reads channel c of this input with getPolyVoltage, multiplies it by 0.2 and Excavi.cpp:521 clamps the knob-plus-input sum to -1 to 1
   Excavi in8 — Excavi.cpp:455 reads channel c of this input with getPolyVoltage and adds it to the knob one for one; Excavi.cpp:457 clamps the sum to -5 to 5
   Excavi in9 — Excavi.cpp:461 reads channel c of this input with getPolyVoltage and adds it to the knob one for one; Excavi.cpp:463 clamps the sum to -5 to 5
   Fauna in2 — Fauna.cpp:179 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 1
   Fauna in3 — Fauna.cpp:177 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 4
   Retri in2 — Flora.cpp:244 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 1
   Retri in3 — Flora.cpp:242 reads this input with getVoltage, scales it by the attenuator and clamps the knob-plus-input sum to 0 to 4
   Saw2 in2 — Saw2.cpp:193 takes getChannels() on this input and reads channel c with getPolyVoltage; Saw2.cpp:194 multiplies it by 4 and Saw2.cpp:197 clamps the knob-plus-input sum to 0 to 60, so a negative voltage reduces the age to a floor of new
   Trace in1 — Trace.cpp:277 reads channel c of this input with getPolyVoltage when it carries that many; Trace.cpp:278 multiplies by 0.1 and Trace.cpp:279 clamps the knob-plus-input sum to 0 to 10
   Vibrato in1 — Vibrato.cpp:113 reads channel c of this input with getPolyVoltage when it carries more than one channel; Vibrato.cpp:119 clamps the knob-plus-input total to 0.001 to 0.020 seconds
   Vibrato in2 — Vibrato.cpp:112 reads channel c of this input with getPolyVoltage when it carries more than one channel; Vibrato.cpp:118 clamps the knob-plus-input total to 1 to 20Hz
   Vibrato in3 — Vibrato.cpp:114 reads channel c of this input with getPolyVoltage when it carries more than one channel; Vibrato.cpp:120 clamps the knob-plus-input total to 0 to 1

== BlushAudio
   BuffMix in0 — BuffMix.cpp:72-74 and 104-105 read this port with getVoltageSimd<float_4>(ch) inside a loop bounded by the channel count gathered at lines 41-59
   BuffMix in1 — BuffMix.cpp:72-74 and 104-105 read this port with getVoltageSimd<float_4>(ch) inside a loop bounded by the channel count gathered at lines 41-59
   BuffMix in2 — BuffMix.cpp:72-74 and 104-105 read this port with getVoltageSimd<float_4>(ch) inside a loop bounded by the channel count gathered at lines 41-59

== ChowDSP
   ChowModal in2 — ChowModal.cpp adds getVoltage() / 10 to the decay knob and clamps the total to 0 and 1.5 before multiplying by seven seconds
   ChowTape in0 — ChowTape.cpp process(): clamp(inputs[AUDIO_INPUT].getVoltage() / 5.0f, -1.0f, 1.0f), no channel argument

== Coffee
   Quant in1 — Quant.cpp Quant::process: the jack is truncated to a whole number and clamped to -12 to 12; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels

== CosineKitty-Sapphire
   Chaops in4 — chaops_vcv.cpp:139 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Chaops in5 — chaops_vcv.cpp:144 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Echo in3 — echo_vcv.cpp:2300 reads this input per channel at half a knob unit per 5V and clamps the total between 0 and 1
   Echo in4 — echo_vcv.cpp:1168 reads this input at one knob unit per 5V and clamps the total between -1 and +1
   Echo in5 — echo_vcv.cpp:2253 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Echo in6 — echo_vcv.cpp:1092 reads this input per channel at one knob unit per 5V and clamps the total between 0 and 1
   EchoOut in0 — echo_vcv.cpp:3288 reads this input per channel at half a knob unit per 5V and clamps the total between 0 and 1
   EchoOut in1 — echo_vcv.cpp:3286 reads this input per channel at one knob unit per 5V and clamps the total between 0 and 2
   EchoTap in1 — echo_vcv.cpp:1168 reads this input at one knob unit per 5V and clamps the total between -1 and +1
   EchoTap in3 — echo_vcv.cpp:1092 reads this input per channel at one knob unit per 5V and clamps the total between 0 and 1
   Elastika in0 — elastika_vcv.cpp:318 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Elastika in1 — elastika_vcv.cpp:319 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Elastika in2 — elastika_vcv.cpp:320 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Elastika in3 — elastika_vcv.cpp:321 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Elastika in4 — elastika_vcv.cpp:322 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Elastika in8 — elastika_vcv.cpp:325 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Elastika in9 — elastika_vcv.cpp:326 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Empath in2 — empath_vcv.cpp:953 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 1 and 5; with nothing patched the jack reads Empath's own chaos voltage
   Empath in3 — empath_vcv.cpp:986 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1; with nothing patched the jack reads Empath's own chaos voltage
   Empath in4 — empath_vcv.cpp:1011 reads this input and clamps the chaos level knob plus the attenuated voltage between 0 and 2, one knob unit per volt at the attenuverter at 100%
   Empath in5 — empath_vcv.cpp:973 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2; with nothing patched the jack reads Empath's own chaos voltage
   EmpathFilter in1 — empath_vcv.cpp:1957 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1; with nothing patched the jack reads Empath's chaos voltage
   EmpathFilter in2 — empath_vcv.cpp:1798 reads this input at one knob unit per 5V and clamps the total between -1 and +1; with nothing patched it reads Empath's chaos voltage
   EmpathFilter in3 — empath_vcv.cpp:1958 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1; with nothing patched the jack reads Empath's chaos voltage
   EmpathOutput in0 — empath_vcv.cpp:2515 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1; with nothing patched the jack reads Empath's chaos voltage
   EmpathOutput in1 — empath_vcv.cpp:2514 reads this input at one knob unit per 5V and clamps the total between 0 and 2; with nothing patched it reads Empath's chaos voltage
   Env in1 — env_vcv.cpp:154 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -96 and 0
   Env in2 — env_vcv.cpp:150 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Env in4 — env_vcv.cpp:142 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Env in5 — env_vcv.cpp:146 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Frolic in0 — sapphire_chaos_module.hpp:331 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -7 and 7; the speed knob is measured in octaves
   Frolic in1 — sapphire_chaos_module.hpp:329 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Galaxy in2 — galaxy_vcv.cpp:131 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Galaxy in3 — galaxy_vcv.cpp:132 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Galaxy in4 — galaxy_vcv.cpp:133 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Galaxy in5 — galaxy_vcv.cpp:134 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Galaxy in6 — galaxy_vcv.cpp:135 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Glee in0 — sapphire_chaos_module.hpp:331 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -7 and 7; the speed knob is measured in octaves
   Glee in1 — sapphire_chaos_module.hpp:329 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Gravy in3 — gravy_vcv.cpp:160 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Gravy in4 — gravy_vcv.cpp:161 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Gravy in5 — gravy_vcv.cpp:162 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Lark in0 — sapphire_chaos_module.hpp:331 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -7 and 7; the speed knob is measured in octaves
   Lark in1 — sapphire_chaos_module.hpp:329 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Nucleus in3 — nucleus_vcv.cpp:243 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -6 and 6; the speed knob is measured in octaves
   Nucleus in4 — nucleus_vcv.cpp:221 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Nucleus in5 — nucleus_vcv.cpp:250 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Nucleus in6 — nucleus_vcv.cpp:229 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Nucleus in7 — nucleus_vcv.cpp:236 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Pivot in1 — pivot_vcv.cpp:88 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -3 and 3
   Polynucleus in1 — polynucleus_vcv.cpp:252 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -6 and 6; the speed knob is measured in octaves
   Polynucleus in2 — polynucleus_vcv.cpp:226 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Polynucleus in3 — polynucleus_vcv.cpp:259 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1
   Polynucleus in4 — polynucleus_vcv.cpp:236 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Polynucleus in5 — polynucleus_vcv.cpp:245 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Pop in1 — pop_vcv.cpp:206 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Sauce in2 — sauce_vcv.cpp:226 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Sauce in3 — sauce_vcv.cpp:227 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Sauce in4 — sauce_vcv.cpp:228 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 1
   Tin in5 — tin_vcv.cpp:93 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   Tout in0 — tout_vcv.cpp:96 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between 0 and 2
   TubeUnit in0 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 5
   TubeUnit in1 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 1
   TubeUnit in2 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 1
   TubeUnit in3 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 1
   TubeUnit in4 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0.5 and 20
   TubeUnit in5 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between -10 and 10
   TubeUnit in6 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 8
   TubeUnit in7 — tubeunit_vcv.cpp:170 reads this input per channel; line 180 adds the attenuated voltage over five, times the control's span, to the knob and line 182 clamps the total between 0 and 1
   Zoo in0 — sapphire_chaos_module.hpp:331 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -7 and 7; the speed knob is measured in octaves
   Zoo in1 — sapphire_chaos_module.hpp:329 reads this input per channel; sapphire_vcvrack.hpp:1094 adds the attenuated voltage over five, times the control's span, to the knob and clamps the total to that span, so ±5V at the attenuverter at 100% covers the whole control, the total held between -1 and 1

== HetrickCVGPL
   PhasorBeatMap in7 — src/PhasorBeatMap/PhasorBeatMap.cpp:102-105 reads getVoltage() only while connected, multiplies by 0.4 to give one mode per 2.5V, adds the knob and rounds and clamps to 0 to 2
   PhasorWavetable in1 — src/PhasorWavetable/PhasorWavetable.hpp:551 reads getPolyVoltage(c), multiplies it by the trimpot and by 0.1, adds the knob and clamps the total to 0 to 1

== Hora-PCMDrumFree
   Hora-PCMDrumLDrum in6 — LDrum::process +0x484c reads inputs[6].voltages[0], multiplies it by 2.5 and truncates it, then adds it to five times the bank plus the sample with the total clamped to 0 and 24

== JPFree
   ShiftAndShuffle in0 — plugin.dylib ShiftAndShuffle::process at 0x4f3d0 reads only voltages[0] of this port; CustomBtnAndTrig::process at 0x4f754 uses the 2.0V/0.1V gate in Always 1 mode and scales the voltage in the others, with negative voltages clamped away or reversed by the Uni and Bi choice

== JulioChaos
   JulioChua in0 — JulioChua.cpp line 100, getVoltage multiplied by the attenuverter and by rateScale*0.037/sr, then the sum clamped
   JulioChua in1 — JulioChua.cpp line 104, getVoltage multiplied by the attenuverter and by 0.5, then the sum clamped to 0.98 to 1.9
   JulioChua in2 — JulioChua.cpp line 108, getVoltage multiplied by the attenuverter and by -0.04, then the sum clamped to -1.23 to -1.143
   JulioChua in3 — JulioChua.cpp lines 102, 106 and 110, the one voltage read three times and applied to three constants, each clamped separately
   JulioLorentz84 in0 — JulioLorentz84.cpp line 102, getVoltage multiplied by the attenuverter and by rateScale*0.037/sr, then the sum clamped
   JulioLorentz84 in1 — JulioLorentz84.cpp line 104, getVoltage multiplied by the attenuverter and by 1.1, then the sum clamped to 2.6 to 5
   JulioLorentz84 in2 — JulioLorentz84.cpp line 106, getVoltage multiplied by the attenuverter and by 1.2, then the sum clamped to 7.4 to 10
   JulioLorentz84 in3 — JulioLorentz84.cpp line 108, getVoltage multiplied by the attenuverter and by -0.2, then the sum clamped to 0.7 to 1.3
   JulioRossler in0 — JulioRossler.cpp line 96, getVoltage multiplied by the attenuverter and by rateScale*0.037/sr, then the sum clamped
   JulioRossler in1 — JulioRossler.cpp line 98, getVoltage multiplied by the attenuverter and by 0.12, then the sum clamped to 0.05 to 0.33
   JulioRossler in2 — JulioRossler.cpp line 100, getVoltage multiplied by the attenuverter and by -0.34, then the sum clamped to 0.15 to 0.9
   JulioRossler in3 — JulioRossler.cpp line 102, getVoltage multiplied by the attenuverter and by 5, then the sum clamped to 5.5 to 16.5

== Leviathan
   IntegralFlux in0 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; the signal is soft-limited at 8V and mapped across the 0V to 10.2V slew span, so negative voltage is carried in
   IntegralFlux in5 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; the signal is soft-limited at 8V and mapped across the 0V to 10.2V slew span, so negative voltage is carried in
   IntegralFlux in6 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way
   IntegralFlux in7 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way
   IntegralFlux in8 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; bothTimeScaleFromCv soft-limits at 8V and returns a time multiplier either side of the neutral voltage
   IntegralFlux in9 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; bothTimeScaleFromCv soft-limits at 8V and returns a time multiplier either side of the neutral voltage
   IntegralFlux in10 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way
   IntegralFlux in11 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/IntegralFlux.cpp: every jack is read with getVoltage() and nothing in the plugin loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way
   Proc in0 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/Proc.cpp: every jack is read with getVoltage() and nothing in the module loops over an input's channels; the signal is clamped into the 0V to 10V function span in slew mode, and in cycle mode soft-limited and folded in
   Proc in3 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/Proc.cpp: every jack is read with getVoltage() and nothing in the module loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way
   Proc in4 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/Proc.cpp: every jack is read with getVoltage() and nothing in the module loops over an input's channels; bothTimeScaleFromCv soft-limits at 8V and returns a time multiplier either side of the neutral voltage
   Proc in5 — Leviathan 2.9.1 source (commit c37193d, branch puffy-rc3), src/Proc.cpp: every jack is read with getVoltage() and nothing in the module loops over an input's channels; computeStageTime soft-limits the CV at 8V, scales it by STAGE_CV_OCT_PER_V = 0.5 octaves per volt and clamps to CV_OCT_CLAMP = 12 octaves either way

== MADZINE
   Drummmmmmer in0 — src/Drummmmmmer.cpp:328-336, styleValue += getVoltage(), then round and clamp to 0..9
   Drummmmmmer in13 — src/Drummmmmmer.cpp:366-374, decayParam += getVoltage()*0.18, then clamp(0.2, 2)
   EllenRipley in2 — src/EllenRipley.cpp:657-668, delayTimeL += cv*0.2, then clamp(0.001, 2)
   EllenRipley in3 — src/EllenRipley.cpp:670-681, delayTimeR += cv*0.2, then clamp(0.001, 2)
   EllenRipley in4 — src/EllenRipley.cpp:683-694, feedback += cv*0.1, then clamp(0, 0.95)
   EllenRipley in5 — src/EllenRipley.cpp:711-719, grainSize += cv*0.1, then clamp(0, 1)
   EllenRipley in6 — src/EllenRipley.cpp:721-729, grainDensity += cv*0.1, then clamp(0, 1)
   EllenRipley in7 — src/EllenRipley.cpp:731-739, grainPosition += cv*0.1, then clamp(0, 1)
   EllenRipley in8 — src/EllenRipley.cpp:741-749, reverbRoomSize += cv*0.1, then clamp(0, 1)
   EllenRipley in9 — src/EllenRipley.cpp:751-759, reverbDamping += cv*0.1, then clamp(0, 1)
   EllenRipley in10 — src/EllenRipley.cpp:761-769, reverbDecay += cv*0.1, then clamp(0, 1)
   EuclideanRhythm in2 — src/EuclideanRhythm.cpp:497-506, length = round(clamp(lengthParam + getVoltage()*atten, 1, 32))
   EuclideanRhythm in4 — src/EuclideanRhythm.cpp:520-530, shift = round(clamp(shiftParam + getVoltage()*atten, 0, length-1))
   EuclideanRhythm in5 — src/EuclideanRhythm.cpp:497-506 with i = 1, reading TRACK2_LENGTH_CV_INPUT: length = round(clamp(lengthParam + getVoltage()*atten, 1, 32)), one step per volt at the attenuator's full travel
   EuclideanRhythm in7 — src/EuclideanRhythm.cpp:520-530 with i = 1, reading TRACK2_SHIFT_CV_INPUT: shift = round(clamp(shiftParam + getVoltage()*atten, 0, length-1))
   EuclideanRhythm in8 — src/EuclideanRhythm.cpp:497-506 with i = 2, reading TRACK3_LENGTH_CV_INPUT: length = round(clamp(lengthParam + getVoltage()*atten, 1, 32)), one step per volt at the attenuator's full travel
   EuclideanRhythm in10 — src/EuclideanRhythm.cpp:520-530 with i = 2, reading TRACK3_SHIFT_CV_INPUT: shift = round(clamp(shiftParam + getVoltage()*atten, 0, length-1))
   Facehugger in2 — src/Facehugger.cpp:292-300, grainSize += getPolyVoltage*0.1, then clamp(0, 1)
   Facehugger in3 — src/Facehugger.cpp:303-311, grainDensity += getPolyVoltage*0.1, then clamp(0, 1)
   Facehugger in4 — src/Facehugger.cpp:314-322, grainPosition += getPolyVoltage*0.1, then clamp(0, 1)
   Facehugger in5 — src/Facehugger.cpp:336-344, mix += getPolyVoltage*0.1, then clamp(0, 1)
   Facehugger in6 — src/Facehugger.cpp:238-246, chaosAmount += getPolyVoltage*0.1, then clamp(0, 1)
   Facehugger in7 — src/Facehugger.cpp:249-257, chaosRate += getPolyVoltage*0.2, then clamp(0.01, 2)
   KIMO in2 — src/KIMO.cpp:481-488, fmAmount += getVoltage()/10, clamp(0, 1)
   KIMO in3 — src/KIMO.cpp:499-506, punchAmount += getVoltage()/10, clamp(0, 1)
   KIMO in4 — src/KIMO.cpp:462-469, decayParam += getVoltage()/10, clamp(0.01, 2)
   KIMO in5 — src/KIMO.cpp:445-454, fillParam += getVoltage()*10, clamp(0, 100), then rounded to a whole number of steps
   Ovomorph in2 — src/Ovomorph.cpp:288-298, roomSize += getPolyVoltage*0.1, then clamp(0, 1)
   Ovomorph in3 — src/Ovomorph.cpp:300-309, damping += getPolyVoltage*0.1, then clamp(0, 1)
   Ovomorph in4 — src/Ovomorph.cpp:311-320, decay += getPolyVoltage*0.1, then clamp(0, 1)
   Ovomorph in5 — src/Ovomorph.cpp:333-341, mix += getPolyVoltage*0.1, then clamp(0, 1)
   Ovomorph in6 — src/Ovomorph.cpp:236-244, chaosAmount += getPolyVoltage*0.1, then clamp(0, 1)
   Ovomorph in7 — src/Ovomorph.cpp:247-255, chaosRate += getPolyVoltage*0.2, then clamp(0.01, 2)
   Runner in2 — src/Runner.cpp:301-310, timeL += getPolyVoltage*0.2, then clamp(0.001, 2)
   Runner in3 — src/Runner.cpp:314-323, timeR += getPolyVoltage*0.2, then clamp(0.001, 2)
   Runner in4 — src/Runner.cpp:331-341, feedback += getPolyVoltage*0.1, then clamp(0, 0.95)
   Runner in5 — src/Runner.cpp:361-369, mix += getPolyVoltage*0.1, then clamp(0, 1)
   Runner in6 — src/Runner.cpp:248-256, chaosAmount += getPolyVoltage*0.1, then clamp(0, 1)
   Runner in7 — src/Runner.cpp:259-267, chaosRate += getPolyVoltage*0.2, then clamp(0.01, 2)
   TWNC in3 — src/TWNC.cpp:752-759, decayParam += getVoltage()/10, then clamp(0.01, 2)
   TWNC in5 — src/TWNC.cpp:816-823, decayParam += getVoltage()/10, then clamp(0.01, 2), then halved
   TWNC2 in3 — src/TWNC2.cpp:351-359, kickFmAmount += getVoltage()/10*20, clamp(0, 20)
   TWNC2 in4 — src/TWNC2.cpp:341-349, kickPunchAmount += getVoltage()/10, clamp(0, 1)
   TWNC2 in6 — src/TWNC2.cpp:384-392, snareNoiseMix += getVoltage()/10, clamp(0, 1)
   TWNC2 in8 — src/TWNC2.cpp:434-441, hatsDecay += getVoltage()/10, clamp(0, 1)
   TWNC2 out3 — src/TWNC2.cpp:470-476, the sum soft-limited above 5V rather than clamped
   TWNCLight in2 — src/TWNCLight.cpp:544-551, decayParam += getVoltage()/10, then clamp(0.01, 2)
   TWNCLight in4 — src/TWNCLight.cpp:566-573, decayParam += getVoltage()/10, then clamp(0.01, 2), then halved
   WeiiiDocumenta in2 — src/weiiidocumenta.cpp:841-847, scanValue += getVoltage()/10 * atten, then clamp(0, 1)
   WeiiiDocumenta in4 — src/weiiidocumenta.cpp:610-616, feedbackValue += getVoltage()/10 * atten, then clamp(0, 1)
   WeiiiDocumenta in5 — src/weiiidocumenta.cpp:907-911, playbackSpeed += getVoltage(), then clamp(-8, 8)
   WeiiiDocumenta in7 — src/weiiidocumenta.cpp:1196-1201, shGain += getVoltage()*0.5*atten, then clamp(0, 5)
   theKICK in2 — src/theKICK.cpp:523-528, sweep += getVoltage()*50, clamp(0, 1000); the clamp is in Hz, not volts
   theKICK in3 — src/theKICK.cpp:530-535, bend += getVoltage()*0.35, clamp(0.5, 4)
   theKICK in4 — src/theKICK.cpp:537-542, decayMs += getVoltage()*100, clamp(10, 2000)
   theKICK in5 — src/theKICK.cpp:544-549, fold += getVoltage(), clamp(0, 10)
   theKICK in6 — src/theKICK.cpp:558-563, fb += getVoltage()*0.1, clamp(0, 1)
   theKICK in7 — src/theKICK.cpp:565-570, toneKnob += getVoltage(), clamp(0, 10)
   theKICK in8 — src/theKICK.cpp:551-556, sampleMix += getVoltage(), clamp(0, 10)

== ML_modules
   SeqSwitch in8 — SeqSwitch.cpp:128 rescales the clamped voltage across the steps and rounds it to one
   SeqSwitch2 in5 — SeqSwitch2.cpp:148 rescales the clamped voltage across the steps and rounds it to one

== NANOModules
   SERRA in0 — SERRA::process at 0x2540c reads only voltages[0], adds the manual gate button and clamps the sum to 0 to 1 before the envelope compares it against its threshold

== NonLinearInstruments
   BallisticENV in0 — BallisticENV.cpp BallisticENV::process and classes/Ballistic.cpp: the jack is scaled by its small knob and added to the main knob before the result is clamped to 0 to 1, read with getVoltage() and no channel argument, so the module is monophonic, commit 215f17aa, the head of the maker’s repository and the only commit carrying version 2.0.0, whose res/ matches the installed panels
   BallisticENV in1 — BallisticENV.cpp BallisticENV::process and classes/Ballistic.cpp: the jack is scaled by its small knob and added to the main knob before the result is clamped to 0 to 1, read with getVoltage() and no channel argument, so the module is monophonic, commit 215f17aa, the head of the maker’s repository and the only commit carrying version 2.0.0, whose res/ matches the installed panels
   BallisticENV in2 — BallisticENV.cpp BallisticENV::process and classes/Ballistic.cpp: the jack is scaled by its small knob and added to the main knob before the result is clamped to 0 to 1, read with getVoltage() and no channel argument, so the module is monophonic, commit 215f17aa, the head of the maker’s repository and the only commit carrying version 2.0.0, whose res/ matches the installed panels
   BallisticENV in3 — BallisticENV.cpp BallisticENV::process and classes/Ballistic.cpp: the jack is scaled by its small knob and added to the main knob before the result is clamped to 0 to 1, read with getVoltage() and no channel argument, so the module is monophonic, commit 215f17aa, the head of the maker’s repository and the only commit carrying version 2.0.0, whose res/ matches the installed panels

== PatchableDevices
   ComparingCounter in1 — ComparingCounter.cpp:66-68 reads getVoltage() with no channel, multiplies it by the attenuverter and adds it to the counter limit knob, the sum clamped to 0 and 5.17
   DualIntegrator in0 — DualIntegrator.cpp:110-118 reads getVoltage() with no channel and clamps it to -12V and 12V before slewing it
   DualIntegrator in1 — DualIntegrator.cpp:110-118 reads getVoltage() with no channel and clamps it to -12V and 12V before slewing it
   NonlinearIntegrator in3 — NonlinearIntegrator.cpp:76-78 reads getVoltage() with no channel, multiplies it by the attenuverter and adds it to the resonance knob, the sum clamped to 0 and 12
   NonlinearIntegrator in4 — NonlinearIntegrator.cpp:76-80 reads getVoltage() with no channel, multiplies it by the input attenuator and clamps it to -12V and 12V

== PathSet
   AstroVibe in9 — AstroVibe.cpp:417 reads this input with getPolyVoltage(ei) and AstroVibe.cpp:460 divides it by 10, adds the knob and clamps the total to 0..1
   AstroVibe in10 — AstroVibe.cpp:417 reads this input with getPolyVoltage(ei) and AstroVibe.cpp:460 divides it by 10, adds the knob and clamps the total to 0..1
   AstroVibe in11 — AstroVibe.cpp:417 reads this input with getPolyVoltage(ei) and AstroVibe.cpp:460 divides it by 10, adds the knob and clamps the total to 0..1
   Nudge in0 — Nudge.cpp:198 divides this input by 10 into that line's chance of moving when the input CV mode is Chance, or Nudge.cpp:245 clamps it to ±10V and adds it to the output when the mode is Offset
   Nudge in1 — Nudge.cpp:198 divides this input by 10 into that line's chance of moving when the input CV mode is Chance, or Nudge.cpp:245 clamps it to ±10V and adds it to the output when the mode is Offset
   Nudge in2 — Nudge.cpp:198 divides this input by 10 into that line's chance of moving when the input CV mode is Chance, or Nudge.cpp:245 clamps it to ±10V and adds it to the output when the mode is Offset
   Nudge in3 — Nudge.cpp:198 divides this input by 10 into that line's chance of moving when the input CV mode is Chance, or Nudge.cpp:245 clamps it to ±10V and adds it to the output when the mode is Offset
   Nudge in4 — Nudge.cpp:198 divides this input by 10 into that line's chance of moving when the input CV mode is Chance, or Nudge.cpp:245 clamps it to ±10V and adds it to the output when the mode is Offset

== SanguineMonsters
   Sanguine-Alchemist in1 — alchemist.cpp:144 reads this input with getVoltage, divides it by 5, adds the MASTER knob and clamps the total to 0-2
   Sanguine-Alembic in0 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in1 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in2 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in3 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in4 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in5 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in6 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in7 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in8 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in9 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in10 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in11 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in12 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in13 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in14 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Alembic in15 — alchemist.cpp:442 reads this input with getVoltage, divides it by 5, adds the channel fader and clamps the total to 0-2
   Sanguine-Denki in0 — kitsune.cpp:289 reads this input with getVoltageSimd, divides it by 5, adds the ATTEN. knob and clamps the total to -2 to 2
   Sanguine-Denki in1 — kitsune.cpp:289 reads this input with getVoltageSimd, divides it by 5, adds the ATTEN. knob and clamps the total to -2 to 2
   Sanguine-Denki in2 — kitsune.cpp:289 reads this input with getVoltageSimd, divides it by 5, adds the ATTEN. knob and clamps the total to -2 to 2
   Sanguine-Denki in3 — kitsune.cpp:289 reads this input with getVoltageSimd, divides it by 5, adds the ATTEN. knob and clamps the total to -2 to 2
   Sanguine-Denki in4 — kitsune.cpp:293 reads this input with getVoltageSimd, divides it by 5, adds the OFFSET knob and clamps the total to -10V to 10V
   Sanguine-Denki in5 — kitsune.cpp:293 reads this input with getVoltageSimd, divides it by 5, adds the OFFSET knob and clamps the total to -10V to 10V
   Sanguine-Denki in6 — kitsune.cpp:293 reads this input with getVoltageSimd, divides it by 5, adds the OFFSET knob and clamps the total to -10V to 10V
   Sanguine-Denki in7 — kitsune.cpp:293 reads this input with getVoltageSimd, divides it by 5, adds the OFFSET knob and clamps the total to -10V to 10V
   Sanguine-Fortuna in2 — fortuna.cpp:107 reads a numbered channel of this input with getVoltage; fortuna.cpp:111 divides it by 5, adds the slider and clamps the total to 0-1
   Sanguine-Fortuna in3 — fortuna.cpp:107 reads inputs[INPUT_P_1 + section] with getVoltage; fortuna.cpp:111 divides it by 5, adds the slider and clamps the total to 0-1
   Sanguine-Monsters-Chronos in12 — chronos.cpp:253 reads this input with getPolyVoltageSimd, divides it by 10, scales it by the attenuverter and clamps the sum with the knob to 0.01-0.99
   Sanguine-Monsters-Chronos in13 — chronos.cpp:253 reads this input with getPolyVoltageSimd, divides it by 10, scales it by the attenuverter and clamps the sum with the knob to 0.01-0.99
   Sanguine-Monsters-Chronos in14 — chronos.cpp:253 reads this input with getPolyVoltageSimd, divides it by 10, scales it by the attenuverter and clamps the sum with the knob to 0.01-0.99
   Sanguine-Monsters-Chronos in15 — chronos.cpp:253 reads this input with getPolyVoltageSimd, divides it by 10, scales it by the attenuverter and clamps the sum with the knob to 0.01-0.99
   Sanguine-Monsters-Sphinx in0 — sphinx.cpp:249 reads this input with getVoltage, divides it by 9 and adds the STEPS knob, then clamps the total to 0-1
   Sanguine-Monsters-Sphinx in2 — sphinx.cpp:247 reads this input with getVoltage, divides it by 9 and adds the ROT knob, then clamps the total to 0-1
   Sanguine-Monsters-Sphinx in3 — sphinx.cpp:276 reads this input with getVoltage, divides it by 9 and adds the ACC ROT knob, then clamps the total to 0-1
   Sanguine-Monsters-Sphinx in4 — sphinx.cpp:250 reads this input with getVoltage, divides it by 9 and adds the ACC knob, then clamps the total to 0-1
   Sanguine-Monsters-Sphinx in5 — sphinx.cpp:246 reads this input with getVoltage, divides it by 9 and adds the PAD knob, then clamps the total to 0-1
   Sanguine-Werewolf in0 — werewolf.cpp:85 reads a numbered channel of this input with getVoltage and clamps the knob plus it to 0-20
   Sanguine-Werewolf in1 — werewolf.cpp:84 reads a numbered channel of this input with getVoltage and clamps the knob plus it to 0-10

== SchlappiEngineering
   BTFLD in0 — src/btfld.cpp line 185 reads inputs[INPUT_INPUT].getVoltage() with no channel loop; line 202 clamps the gain-staged signal to 0 to 11.7V, so at the +10 range anything below 0V is discarded
   BTFLD in2 — src/btfld.cpp lines 192 and 200: the jack is read with getVoltage and added to the gain-staged signal before the 0 to 11.7V clamp at line 202

== SonusModular
   Chainsaw in1 — chainsaw.cpp:108 and :111 read this input with getVoltage and divide it by 5 before adding it to that oscillator's shape knob; the two halves of the mix are each clamped to 0 to 1
   Chainsaw in3 — chainsaw.cpp:108 and :111 read this input with getVoltage and divide it by 5 before adding it to that oscillator's shape knob; the two halves of the mix are each clamped to 0 to 1
   Chainsaw in5 — chainsaw.cpp:108 and :111 read this input with getVoltage and divide it by 5 before adding it to that oscillator's shape knob; the two halves of the mix are each clamped to 0 to 1
   Fluidmix in0 — fluidmix.cpp:61 reads this input with getVoltage and multiplies it by 0.3; fluidmix.cpp:62 clamps the knob-plus-input sum to 1 to 4
   Mrcheb in0 — mrcheb.cpp:127 reads this input with getVoltage, which is channel 0 only; mrcheb.cpp:138 to :163 clamp it to ±5V or 0 to 10V depending on the switch
   Neurosc in1 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in2 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in3 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in4 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in5 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in6 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in7 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Neurosc in8 — neurosc.cpp:103 reads this input with getVoltage and adds it to the knob one for one; neurosc.cpp:104 clamps the sum to 0 to 10
   Oktagon in0 — oktagon.cpp:84 reads this input with getVoltage and raises ten to the power of a fifth of it in the LFO range; oktagon.cpp:89 adds it to the knob as octaves in the OSC range and oktagon.cpp:90 clamps that sum to -4 to 4
   Scramblase in3 — scramblase.cpp:79 reads this input with getVoltage and divides it by 5; scramblase.cpp:80 to :87 clamp the knob-plus-input sum to 0 to 1
   Tropicana in2 — tropicana.cpp:114 reads this input with getVoltage, multiplies it by 0.2 and clamps the knob-plus-input sum to -1 to 1
   Tropicana in4 — tropicana.cpp:114 reads this input with getVoltage, multiplies it by 0.2 and clamps the knob-plus-input sum to -1 to 1
   Tropicana in6 — tropicana.cpp:114 reads this input with getVoltage, multiplies it by 0.2 and clamps the knob-plus-input sum to -1 to 1
   Tropicana in8 — tropicana.cpp:114 reads this input with getVoltage, multiplies it by 0.2 and clamps the knob-plus-input sum to -1 to 1
   Tropicana in10 — tropicana.cpp:114 reads this input with getVoltage, multiplies it by 0.2 and clamps the knob-plus-input sum to -1 to 1

== Sparkette
   PolyCat out0 — src/PolyCat.cpp lines 74-75, nchan summed over the patched inputs and clamped to 16

== Sulamith
   Compare in0 — Compare.cpp Compare::process: clamp(knob + getVoltage(i) * attenuator, -10.f, 10.f) inside the per-channel loop, so each channel of this jack is read against the matching channel of the signal, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   SH in0 — SH.cpp SH::process: getChannels() on this jack sets the channel count while the channel knob is at 0, and getVoltage(i) is read per channel and clamped to ±10V, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   SH in2 — SH.cpp SH::process: getChannels() on this jack sets the channel count while the channel knob is at 0, and getVoltage(i) is read per channel and clamped to ±10V, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   SH in4 — SH.cpp SH::process: getChannels() on this jack sets the channel count while the channel knob is at 0, and getVoltage(i) is read per channel and clamped to ±10V, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   VoltM in0 — VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   VoltM in1 — VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels
   VoltM in2 — VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels

== VectorModular
   SoloMixer in0 — src/SoloMixer.cpp:237 reads one channel with getVoltage when connected and substitutes a constant 5.0f when not; :242 multiplies by the squared knob and :244-246 shape and clamp the result
   baseTrigs in0 — src/baseTrigs.cpp:344-370 reads one channel with getVoltage only while the jack is connected, multiplies it by the 0 to 1 attenuator, then multiplies the tempo by (1 + mod) for positive values and divides by (1 - mod) for negative ones; the resulting tempo is clamped at :369

== dBiz
   FourSeq in3 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in4 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in5 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in6 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in7 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in8 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in9 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   FourSeq in10 — 4Seq.cpp:235 and 239 add this voltage to its knob and clamp the sum to plus or minus 3V, with getVoltage
   Verbo in6 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in7 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in8 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in9 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in10 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in11 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in12 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage
   Verbo in13 — Verbo.cpp:568 adds this voltage to its harmonic knob and clamps the sum to 0 to 1, with getVoltage

== gregsbrain
   dSpec in6 — dspec::BSP::PollAnalogControls at 0x5730-0x5740 reads inputs[6].voltages[0] only when the port is connected and adds it at 0.1 per volt straight to the Transpose knob, with no attenuator in the path, clamped to -1 and 1

== voxglitch
   GrainEngineMK2 in2 — GrainEngineMK2.hpp:306 divides this input by 10, multiplies by the attenuverter and adds the knob, and GrainEngineMK2.hpp:307 clamps the total to 0..1
   GrainEngineMK2 in8 — GrainEngineMK2.hpp:256 divides this input by 10 and scales it by the attenuverter and 4, and GrainEngineMK2.hpp:261 clamps the knob-plus-input total to 0..4 before storing it in an unsigned int
   GrainEngineMK2Expander in4 — GrainEngineMK2Expander.hpp:118 stores this input's voltage in an unsigned int, one slot per volt, then adds the knob and clamps to 0..4
   arpseq in4 — RateModel.hpp:17 divides this input by the attenuverter range, multiplies by the attenuverter and RateModel.hpp:22 truncates the total to a rate index clamped to -8..+7
   arpseq in5 — ShapeModel.hpp:20 divides this input by the attenuverter range, multiplies by the attenuverter and ShapeModel.hpp:22 truncates the total to a playback mode clamped to 0..9
   autobreak in3 — Autobreak.hpp:120 divides this input by 10 and scales it by the sample count and the attenuverter before adding the knob; Autobreak.hpp:129 stores the total in an unsigned int and Autobreak.hpp:130 clamps it afterwards
   bytebeat in0 — ByteBeat.hpp:144 scales this input by a tenth of the 128 maximum, multiplies by the attenuverter and ByteBeat.hpp:154 clamps the knob-plus-input total at 0; the result is stored in the uint32_t p1 at ByteBeat.hpp:28
   bytebeat in1 — ByteBeat.hpp:144 scales this input by a tenth of the 128 maximum, multiplies by the attenuverter and ByteBeat.hpp:154 clamps the knob-plus-input total at 0; the result is stored in the uint32_t p2 at ByteBeat.hpp:29
   bytebeat in2 — ByteBeat.hpp:144 scales this input by a tenth of the 128 maximum, multiplies by the attenuverter and ByteBeat.hpp:154 clamps the knob-plus-input total at 0; the result is stored in the uint32_t p3 at ByteBeat.hpp:30
   ghosts in2 — Ghosts.hpp:173 multiplies this input by the attenuverter, adds the knob and Ghosts.hpp:174 clamps the total to 0..10 volts
   ghosts in4 — Ghosts.hpp:169 multiplies this input by the attenuverter, adds the knob and Ghosts.hpp:170 clamps the total to 0..10 volts
   maya in1 — Maya.hpp:266 divides this input by 10 and multiplies by the attenuverter, and Maya.hpp:269 clamps the knob-plus-input total to 0..1 before Maya.hpp:280 turns it into a word index
   repeater in1 — Repeater.hpp:114 divides this input by 10 and Repeater.hpp:168 clamps the knob-plus-input total to 0..1, the same span a tenth of 0 to 10V covers
   repeater in2 — Repeater.hpp:114 divides this input by 10 and scales it by 10, and Repeater.hpp:158 clamps the knob-plus-input total to 0..10
   repeater in3 — Repeater.hpp:129 stores the knob-plus-input total in an unsigned int before Repeater.hpp:130 clamps it to the sample count
   satanonaut in2 — Satanonaut.hpp:122 divides this input by 10 and multiplies by the knob into an int, and Satanonaut.hpp:129 clamps that to 0..the effect count
   wavbankmc in1 — WavBankMC.hpp:505 divides this input by 10 and scales it by the sample count and the attenuverter before adding the knob; WavBankMC.hpp:377 stores the total in an unsigned int and clamps it afterwards
   wavbankmc in6 — WavBankMC.hpp:172 divides this input by 10 and WavBankMC.hpp:174 clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   wavbankmc in7 — WavBankMC.hpp:185 divides this input by 10 and WavBankMC.hpp:187 clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   wavbankmc in8 — WavBankMC.hpp:198 divides this input by 10 and WavBankMC.hpp:200 clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers
   wavbankmc in9 — WavBankMC.hpp:211 divides this input by 10 and WavBankMC.hpp:213 clamps the knob-plus-input total to 0..1, the span a tenth of 0 to 10V covers

