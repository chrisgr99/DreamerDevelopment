# Ports whose own citation names a bound while the range field is blank.
# 392 ports across 30 plugins.

== Ahornberg
   TapeRecorder in1 — processLoopInput() in TapeRecorder.cpp: loopStart = (int)(getVoltage() * 12 + 0.5 + knob), then clamped up to 0
   TapeRecorder in2 — processLoopInput() in TapeRecorder.cpp: loopEnd = (int)(getVoltage() * 12 + 0.5 + knob), then clamped up to 0
   XGlide in3 — XGlide.cpp clamps getVoltage(i) here to -100 and 100 and crossfades it like the pitch

== Ambivalent-Instruments
   Gurgling in3 — the maker's manual page for this module at tsugumasa320.github.io/Ambivalent-Instruments-Manual gives the width law as clamp(knob_width + CV_volts * 0.2, 0, 2), the same as Rain's
   Rain in3 — the maker's manual page for this module at tsugumasa320.github.io/Ambivalent-Instruments-Manual gives the width law as clamp(knob + CV * 0.2, 0, 2), so 5V of CV moves the width by one unit

== BGal256
   DressMeUp in3 — DressMeUp.cpp:176-183 maps this voltage across the range the Step input range switch sets at lines 141-155, clamps it to 0..1 and multiplies by the step count

== Bogaudio
   Bogaudio-EightFO in0 — EightFO.cpp:80 clamp(getPolyVoltage(c)/5,-1,1) for pulse width, or :96 the magnitude over 0 to 5V for sample-and-hold
   Bogaudio-EightFO out0 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out1 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out2 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out3 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out4 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out5 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out6 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-EightFO out7 — EightFO.cpp:176 and 210 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-FourFO in0 — FourFO.cpp:72 clamp(getPolyVoltage(c)/5,-1,1) for pulse width, or :88 the magnitude over 0 to 5V for sample-and-hold
   Bogaudio-FourFO out0 — FourFO.cpp:156 and 190 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-FourFO out1 — FourFO.cpp:156 and 190 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-FourFO out2 — FourFO.cpp:156 and 190 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-FourFO out3 — FourFO.cpp:156 and 190 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO in2 — LFO.cpp:110 clamp(getPolyVoltage(c)/5,-1,1) for offset, or :98 clamp(getPolyVoltage(c)/10,0,1) when the menu points it at smoothing
   Bogaudio-LFO out0 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO out1 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO out2 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO out3 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO out4 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LFO out5 — LFO.cpp:149 and 157 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-LLFO out0 — LLFO.cpp:188-189 setChannels(_channels) then a per-channel write, clamped to ±12V; channels() takes the width from the V/OCT input
   Bogaudio-PolyOff16 out0 — PolyOff16.cpp:24 and 45 setChannels(IN channels) then a per-channel write clamped to ±12V
   Bogaudio-PolyOff8 out0 — PolyOff8.cpp:24 and 45 setChannels(IN channels, up to 8) then a per-channel write clamped to ±12V
   Bogaudio-Sums out0 — Sums.cpp:8 and 13 setChannels then a per-channel write of A plus B, clamped to ±12V unless the output limit is disabled on the menu; the A input sets the width
   Bogaudio-Sums out1 — Sums.cpp:9 and 14 setChannels then a per-channel write of A minus B, clamped to ±12V unless the output limit is disabled on the menu; the A input sets the width
   Bogaudio-Sums out2 — Sums.cpp:10 and 15 setChannels then a per-channel write of the larger of A and B, clamped to ±12V unless the output limit is disabled on the menu
   Bogaudio-Sums out3 — Sums.cpp:11 and 16 setChannels then a per-channel write of the smaller of A and B, clamped to ±12V unless the output limit is disabled on the menu

== CVfunk
   Aulos in10 — Aulos.cpp:1136 reads it with getVoltage, so only channel 1, and clamps the knob-plus-input total to -1 to 1
   Cartesia in9 — Cartesia.cpp:493 reads it with getVoltage, so only channel 1, adds it to the minimum knob through its attenuverter and clamps the total to -10 to 10V
   Cartesia in10 — Cartesia.cpp:499 reads it with getVoltage, so only channel 1, adds it to the range knob through its attenuverter and clamps the total to 1 to 20
   Cartesia in17 — Cartesia.cpp:684 reads it with getVoltage, so only channel 1, adds it to the layer voltage and clamps the total to -10 to 10V
   Clpy in0 — Clpy.cpp:189 takes getChannels() on this input and Clpy.cpp:206 reads each channel; Clpy.cpp:210 clamps the knob-plus-input total to 0 to 10
   FlowerPatch in2 — FlowerPatch.cpp:363 reads it with getVoltage, so only channel 1, and scales it by a tenth; the knob-plus-input total is clamped to -1 to 1
   FlowerPatch in3 — FlowerPatch.cpp:209 reads it with getVoltage, so only channel 1, and clamps the knob-plus-input total to -5 to 5V
   FlowerPatch in4 — FlowerPatch.cpp:222 reads it with getVoltage, so only channel 1, and scales it by a tenth; the total is clamped to -1 to 1.1
   Glass in4 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:403 clamps the total to 0 to 6
   Glass in5 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:408 clamps the total to 0 to 1
   Glass in6 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:512 clamps the total to 0 to 1
   Glass in7 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:369 clamps the total to 0 to 1
   Glass in8 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:466 clamps the total to 0 to 1
   Glass in9 — Glass.cpp:233 reads channel one with getPolyVoltage and scales it by a tenth through the attenuverter; Glass.cpp:443 scales the total and clamps it to half an octave either way
   Glass in10 — Glass.cpp:429 reads it with getVoltage, so only channel 1, and scales it by a tenth through the attenuverter; Glass.cpp:431 clamps the total to 0 to 1
   Glass in13 — Glass.cpp:437 reads it with getVoltage, so only channel 1, and scales it by a tenth through the attenuverter; Glass.cpp:439 clamps the total to 0 to 1
   Glass in14 — Glass.cpp:469 reads it with getVoltage, so only channel 1, adds it to the root knob and clamps the total to -2 to 2 octaves
   Haze in2 — Haze.cpp:318 reads it with getVoltage, so only channel 1, and scales it by a tenth; Haze.cpp:365 clamps the knob-plus-input total to 0 to 1
   Haze in3 — Haze.cpp:318 reads it with getVoltage, so only channel 1, and scales it by a tenth; Haze.cpp:366 clamps the knob-plus-input total to 0 to 1
   Haze in4 — Haze.cpp:318 reads it with getVoltage, so only channel 1, and scales it by a tenth; Haze.cpp:354 clamps the knob-plus-input total to 0 to 1
   Haze in5 — Haze.cpp:318 reads it with getVoltage, so only channel 1, and scales it by a tenth; Haze.cpp:355 clamps the knob-plus-input total to 0 to 1
   Haze in6 — Haze.cpp:318 reads it with getVoltage, so only channel 1, and scales it by a tenth; Haze.cpp:357 clamps the knob-plus-input total to a fifth through twice
   HexMod in6 — HexMod.cpp:268 reads it with getVoltage, so only channel 1; HexMod.cpp:277 clamps the knob-plus-input total to 0.00001 to 20
   HexMod in7 — HexMod.cpp:283 reads it with getVoltage, so only channel 1; HexMod.cpp:285 clamps the knob-plus-input total to 0 to 3
   HexMod in9 — HexMod.cpp:406 reads it with getVoltage, so only channel 1; HexMod.cpp:408 clamps the knob-plus-input total to -10 to 10V
   Hub in0 — Hub.cpp:115 takes getChannels() on this input and Hub.cpp:150 reads each channel; Hub.cpp:152 clamps the gain-and-offset result times the range to -10 to 10V
   Magnets in2 — Magnets.cpp:141 reads it with getVoltage, so only channel 1, and scales it by a tenth; Magnets.cpp:147 clamps the knob-plus-input total to 0 to 1
   Magnets in3 — Magnets.cpp:142 reads it with getVoltage, so only channel 1, and scales it by a tenth; Magnets.cpp:148 clamps the knob-plus-input total to -1 to 1
   Magnets in4 — Magnets.cpp:144 reads it with getVoltage, so only channel 1, and scales it by a tenth; Magnets.cpp:150 clamps the knob-plus-input total to 0 to 1
   Morta in0 — Morta.cpp:87 takes getChannels() on this input and Morta.cpp:133 reads each channel; Morta.cpp:134 clamps the gain-and-offset total to -10 to 10V
   Onion in0 — Onion.cpp:109 reads it with getVoltage, so only channel 1; Onion.cpp:110 clamps the knob-plus-input total to -10 to 10V
   Ouros in8 — Ouros.cpp:225 checks getChannels() on this input and Ouros.cpp:256 reads each channel; Ouros.cpp:265 clamps the total to 10
   PentaSequencer in2 — PentaSequencer.cpp:290 reads it with getVoltage and adds it to every output; PentaSequencer.cpp:294 clamps the total to -10 to 10V
   Picus in2 — Picus.cpp:359 reads it with getVoltage, so only channel 1, adds it to the pattern knob through the attenuverter; Picus.cpp:362 rounds up and clamps the total to 1 to 10
   PreeeeeeeeeeessedDuck in68 — PreeeeeeeeeeessedDuck.cpp:966 reads it with getVoltage, so only channel 1; PreeeeeeeeeeessedDuck.cpp:968 clamps the knob-plus-input total to 0 to 1
   PreeeeeeeeeeessedDuck in69 — PreeeeeeeeeeessedDuck.cpp:1082 reads it with getVoltage, so only channel 1; the knob-plus-input total is cubed and clamped to 0 to 11
   PreeeeeeeeeeessedDuck in70 — PreeeeeeeeeeessedDuck.cpp:1113 reads it with getVoltage, so only channel 1, and scales it by a tenth through the attenuverter; PreeeeeeeeeeessedDuck.cpp:1115 clamps the total to 0 to 2
   PressedDuck in28 — PressedDuck.cpp:865 reads it with getVoltage, so only channel 1; PressedDuck.cpp:867 clamps the knob-plus-input total to 0 to 1
   PressedDuck in29 — PressedDuck.cpp:1053 reads it with getVoltage, so only channel 1; the knob-plus-input total is cubed and clamped to 0 to 11
   PressedDuck in30 — PressedDuck.cpp:1012 reads it with getVoltage, so only channel 1, and scales it by a tenth through the attenuverter; PressedDuck.cpp:1014 clamps the total to 0 to 2
   Ranges in0 — Ranges.cpp:77 takes getChannels() on this input and Ranges.cpp:99 reads it with getPolyVoltage; Ranges.cpp:104 clamps the knob-plus-input total to -10 to 10V
   Ranges in1 — Ranges.cpp:78 takes getChannels() on this input and Ranges.cpp:102 reads it with getPolyVoltage; Ranges.cpp:105 clamps the knob-plus-input total to -10 to 10V
   Ranges in2 — Ranges.cpp:85 reads it with getVoltage, so only channel 1, scales it by 2.4 and floors the total, which Ranges.cpp:88 clamps to 0 to 12
   Steps in1 — Steps.cpp:157 reads it with getVoltage, so only channel 1; Steps.cpp:168 clamps the knob-plus-input total to -10 to 10V
   Steps in2 — Steps.cpp:161 reads it with getVoltage, so only channel 1; Steps.cpp:169 takes the absolute value of the knob-plus-input total and clamps it to 0 to 10V
   Steps in4 — Steps.cpp:165 reads it with getVoltage, so only channel 1; Steps.cpp:170 clamps the knob-plus-input total to -5 to 5V
   Strata in2 — Strata.cpp:1512 reads it with getVoltage, so only channel 1, and adds it to the note; Strata.cpp:1528 clamps the total to -10 to 10V
   Strata in4 — Strata.cpp:988 reads it with getVoltage, so only channel 1, adds it to the pattern knob; Strata.cpp:990 rounds up and clamps the total to 1 to 24
   Syncro in1 — Syncro.cpp:239 reads it with getVoltage, so only channel 1, and scales it by 10 through the attenuverter; Syncro.cpp:240 clamps the total to -99 to 99
   Syncro in3 — Syncro.cpp:241 reads it with getVoltage, so only channel 1, and scales it by a tenth through the attenuverter; Syncro.cpp:242 clamps the total to 0.001 to 0.999
   Tatami in3 — Tatami.cpp:297 checks getChannels() on this input and Tatami.cpp:338 reads each channel, scaling by a tenth; Tatami.cpp:339 clamps the total to 0 to 1
   Tatami in4 — Tatami.cpp:298 checks getChannels() on this input and Tatami.cpp:345 reads each channel; Tatami.cpp:346 clamps the knob-plus-input total to -5 to 5V
   TriDelay in3 — TriDelay.cpp:307 reads it with getVoltage, so only channel 1; TriDelay.cpp:317 clamps the knob-plus-input total to -1 to 1
   TriDelay in6 — TriDelay.cpp:311 reads it with getVoltage, so only channel 1, and scales it by a hundredth; TriDelay.cpp:318 clamps each tap's feedback to 0 to 0.99
   TriDelay in7 — TriDelay.cpp:315 reads it with getVoltage, so only channel 1, and scales it by a hundredth; TriDelay.cpp:299 clamps the total to 0 to 1
   Triton in4 — Triton.cpp:429 scales it by a tenth through the trimmer and clamps the total to 0 to 1; Triton.cpp:518 reads each voice
   Triton in5 — Triton.cpp:431 scales it by a fifth through the trimmer and clamps the total to -2 to 2; Triton.cpp:519 reads each voice
   Triton in6 — Triton.cpp:433 scales it by a tenth through the trimmer and clamps the total to 0 to 1; Triton.cpp:520 reads each voice
   Triton in7 — Triton.cpp:435 scales it by a tenth through the trimmer and clamps the total to 0 to 1; Triton.cpp:521 reads each voice
   Triton in8 — Triton.cpp:437 scales it by a tenth through the trimmer and clamps the total to 0 to 1; Triton.cpp:522 reads each voice
   Triton in9 — Triton.cpp:721 reads each voice with getPolyVoltage through the trimmer and clamps the total to 0 to 1
   Triton in10 — Triton.cpp:722 reads each voice with getPolyVoltage through the trimmer and clamps the total to 0 to 1
   Triton in11 — Triton.cpp:723 reads each voice with getPolyVoltage through the trimmer and clamps the total to 0 to 1
   Triton in12 — Triton.cpp:724 reads each voice with getPolyVoltage through the trimmer and clamps the total to 0 to 1
   Triton in13 — Triton.cpp:439 scales it by a tenth through the trimmer and clamps the total to -1 to 1; Triton.cpp:523 reads each voice
   Wonk in2 — Wonk.cpp:304 reads it with getVoltage, so only channel 1, scales it by a tenth and clamps the knob-plus-input total to 0 to 1
   Wonk in4 — Wonk.cpp:275 reads it with getVoltage, so only channel 1, halves it and clamps the knob-plus-input total to -5 to 5V

== CatroModulo
   CatroModulo_CM-1 in8 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in9 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in10 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in11 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in12 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in13 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in14 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in15 — CatroModulo_CM-1.cpp:216 adds getVoltage()*2.1 to the knob's exponent and clamps the total to -8 and 13; lines 211 and 213 do the same with 0.8 and 0.2 when the sync jack is patched
   CatroModulo_CM-1 in16 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in17 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in18 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in19 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in20 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in21 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in22 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in23 — CatroModulo_CM-1.cpp:218 computes knob*(1+getVoltage())*0.05 and clamps it to 0 and 1
   CatroModulo_CM-1 in33 — CatroModulo_CM-1.cpp:197 passes the voltage to CM_BpmClock::setcv, and CatroModulo.hpp:642 turns a volt into 60 bpm while :581 clamps the tempo at zero
   CatroModulo_CM-3 in22 — CatroModulo_CM-3.cpp:138 passes the voltage to CM_BpmClock::setcv, and CatroModulo.hpp:642 turns a volt into 60 bpm while :581 clamps the tempo at zero
   CatroModulo_CM-4 in1 — CatroModulo_CM-4.cpp:82 passes the voltage to CM_BpmClock::addcv, which at CatroModulo.hpp:590 adds cv*60 to the tempo and clamps the result at zero
   CatroModulo_CM-5 in0 — CatroModulo_CM-5.cpp:59-63 pass the voltage to CM_BpmClock::setcv, and CatroModulo.hpp:642 turns a volt into 60 bpm while :581 clamps the tempo at zero
   CatroModulo_CM-7 in0 — CatroModulo_CM-7.cpp:59-63 pass the voltage to CM_BpmClock::setcv, and CatroModulo.hpp:642 turns a volt into 60 bpm while :581 clamps the tempo at zero

== CuteLab
   mom-jeans in5 — CuteLab 2.1.0-rev.0 source (github.com/cutelabnyc/cute-vcv, commit 98d7049), src/mom-jeans.cpp: process() takes the channel count from the largest getChannels() across all eight inputs, sets both outputs to it, and reads this jack with getPolyVoltage(c) inside that loop; pitch += linear_fm * fm_index * pitch * 0.2f, the result clamped to 1Hz to 20000Hz

== DigitalAtavism
   Blip in3 — Blip::process +0x55bc reads inputs[3].voltages[0] into exp2f and multiplies the knob by the result, clamped to 0.0005 and 0.5 seconds; read only on the frame the trigger rises
   Blip in4 — Blip::process +0x5604 reads inputs[4].voltages[0] into exp2f and multiplies the knob by the result, clamped to 0.5 seconds; read only on the frame the trigger rises
   Coin in2 — Coin::process +0x9f7c reads inputs[2].voltages[0] into exp2f and multiplies the knob by the result, clamped at +0x9f94 to 0.8 seconds
   Coin in3 — Coin::process +0x9fbc reads inputs[3].voltages[0] into exp2f and multiplies the knob by the result, clamped to 0.0005 and 0.5 seconds
   Coin in4 — Coin::process +0xa008 reads inputs[4].voltages[0] into exp2f and multiplies the knob by the result, clamped to 0.5 seconds
   DTrig in0 — DTrig::process +0xc868 reads inputs[0].voltages[0] into exp2f and +0xc870 multiplies the DLAY knob by the result, clamped at +0xc878 to 5 seconds; read only on the frame the trigger rises
   DTrig in2 — DTrig::process +0xc918 reads inputs[2].voltages[0] into exp2f and +0xc920 multiplies the TIME knob by the result, clamped at +0xc928 and +0xc938 to 5 seconds and 0.001 seconds; read only when the gate starts
   Hit in4 — Hit::process +0xe794 reads inputs[4].voltages[0] into exp2f and multiplies the HOLD knob by the result, clamped to 0.0005 and 0.5 seconds; read only on the frame the trigger rises
   Hit in5 — Hit::process +0xe7e0 reads inputs[5].voltages[0] into exp2f and multiplies the REL knob by the result, clamped to 0.5 seconds; read only on the frame the trigger rises

== FrequencyDomain
   DelayedReaction in0 — src/controller/DelayedReaction.cpp:290 reads this jack with a bare getVoltage() and line 666 multiplies it by the window value before the FFT, with no scaling or clamp, so no range can be stated. getVoltage() reads channel 1 only.
   DelayedReaction in1 — src/controller/DelayedReaction.cpp:764 tests isConnected() and takes the jack’s raw voltage as the feedback when a cable is in; line 767 uses the FB SEND value instead when it is not, and that value is clamped to ±10V at line 761. getVoltage() reads channel 1 only.
   HarmonicConvergence in0 — src/controller/HarmonicConvergence.cpp:275 reads this jack with a bare getVoltage(); line 315 adds the feedback and multiplies by the window value before the FFT, with no scaling or clamp. Line 272 tests isConnected() to decide whether this side is morphed with the other.
   HeatOfTheMoment out0 — src/controller/HeatOfTheMoment.cpp:122 sets one voltage with setVoltage() and never calls setChannels(), so the jack stays monophonic. The value is raw noise times two window values and is not clamped, so no range can be stated.
   Morphology in0 — src/controller/Morphology.cpp:210 reads this jack with a bare getVoltage() and line 397 multiplies it by the window value before the FFT, with no scaling or clamp, so no range can be stated. getVoltage() reads channel 1 only.
   Morphology in1 — src/controller/Morphology.cpp:211 reads this jack with a bare getVoltage() and line 398 multiplies it by the window value before the second FFT. Nothing is normalled to it and there is no scaling or clamp. getVoltage() reads channel 1 only.

== Grayscale
   Supercell in6 — Grayscale 2.1.0 binary: Supercell::process adds up every channel of this port in a loop bounded by its channels byte
   Supercell in7 — Grayscale 2.1.0 binary: Supercell::process adds up every channel of this port in a loop bounded by its channels byte

== HetrickCV
   GateDelay in2 — GateDelay.cpp:100 reads it per channel and clamps the resulting delay in seconds, not in volts
   GateDelay in3 — GateDelay.cpp:104 reads it per channel and clamps the resulting width in seconds, not in volts
   PhaseDrivenSequencer in1 — PhaseDrivenSequencer.cpp:172 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhaseDrivenSequencer32 in1 — PhaseDrivenSequencer32.cpp:172 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhasorEuclidean in1 — PhasorEuclidean.cpp:173 clamps the knob plus this CV to 1 to 64 beats and passes the float to setBeats
   PhasorEuclidean in2 — PhasorEuclidean.cpp:175 clamps the knob plus this CV to 0 to 64 and passes the float to setFill
   PhasorEuclidean in5 — PhasorEuclidean.cpp:177 multiplies this voltage by fillCVDepth and adds it to the fill mode knob, clamped to 0 to 5; fillCVDepth comes from FILL_SCALE_PARAM at :149, scaled by BEATS_CV_SCALE of 64/5, so one fill mode is a step of about 0.39V at full fill depth. The module's own Fill Limit Mode CV Depth knob is read into fillModeCVDepth at :157 and used nowhere
   PhasorGates in1 — PhasorGates.cpp:148 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhasorGates32 in1 — PhasorGates32.cpp:167 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhasorGates64 in1 — PhasorGates64.cpp:164 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhasorRhythmGroup in1 — PhasorRhythmGroup.cpp:122 clamps the knob plus this CV to 1 to 64 steps and keeps the float
   PhasorRhythmGroup in2 — PhasorRhythmGroup.cpp:131 clamps the knob plus this CV to 1 to 64 and keeps the float
   PhasorRhythmGroup in3 — PhasorRhythmGroup.cpp:134 clamps the knob plus this CV to 1 to 64 and keeps the float
   PhasorShape in2 — PhasorShape.cpp:88 multiplies this voltage by modeDepth, the Shape Mode CV Depth attenuverter times 9/5 (:79), adds it to the mode knob, and :89 truncates the sum to an int clamped to 0 to 9
   PhasorSplitter in1 — PhasorSplitter.cpp:104 clamps the knob plus this CV and hands it to setNumberSteps, which takes an int (DSP/Phasors/HCVPhasorAnalyzers.h:91)
   PhasorSplitter in2 — PhasorSplitter.cpp:116 multiplies this voltage by the Gate Width CV Depth attenuverter (:95) and adds it to the mode knob; :117 truncates the sum to an int clamped to 0 to 4
   PhasorSubstepShape in3 — PhasorSubstepShape.cpp:101 multiplies this voltage by modeDepth, the Mode CV Depth attenuverter times 9/5 (:94), adds it to the mode knob, and :102 truncates the sum to an int clamped to 0 to 9
   Rungler out8 — Rungler.cpp:136 writes the rungler value times the SCALE amount per channel, clamped to ±5V at Rungler.cpp:134
   TrigShaper in1 — TrigShaper.cpp:98 multiplies this voltage by 0.4, adds it to the function knob and truncates to an int, clamped to 0 to 2, so one function is a step of 2.5V
   TrigShaper in2 — TrigShaper.cpp:99 multiplies this voltage by 0.4, adds it to the mode knob and truncates to an int, clamped to 0 to 2, so one mode is a step of 2.5V

== IggyLabsModules
   more-ideas in2 — more-ideas.cpp:136 adds getVoltageSum() to the rule knob when connected, and line 138 clamps the total to 0 and 255 before line 140 takes its floor
   more-ideas in3 — more-ideas.cpp:150 adds getVoltageSum() to the seed knob when connected, and line 152 clamps the total to 0 and 255 before line 154 takes its floor

== ImpromptuModular
   Adaptive-Quantizer in6 — AdaptiveQuantizer.cpp:123-124 reads channel one of this input only (getVoltage with no channel), multiplies it by 1.1 onto the knob and rounds the sum to a whole number of pitches, clamped to 1..12

== MicMusic
   Cuter in2 — Distortion.cpp:14 reads inputs[Inputs::SIGNAL].getVoltage() with no channel argument, and line 21 clamps it between the two levels, which may both be negative

== NonlinearCircuits
   DoubleNeuron in0 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in1 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in2 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in3 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in4 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in5 — DoubleNeuron.cpp:89-90 read this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SNS to 0 to 10V
   DoubleNeuron in6 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in7 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in8 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in9 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in10 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in11 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in12 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   DoubleNeuron in13 — DoubleNeuron.cpp:98-102 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   GENiE in0 — Genie.cpp:98-105 read this input with getVoltage or fall back to neuron 3; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in1 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in2 — Genie.cpp:108-115 read this input with getVoltage or fall back to neuron 1; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in3 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in4 — Genie.cpp:118-125 read this input with getVoltage or fall back to neuron 2; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in5 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in6 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in7 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in8 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   GENiE in9 — Genie.cpp:127-132 read this input with getVoltage; NLCShared.hpp:63 clamps the mix to 0 to 10V
   LetsSplosh in0 — LetsSplosh.cpp:63-66 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   LetsSplosh in1 — LetsSplosh.cpp:63-66 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   LetsSplosh in2 — LetsSplosh.cpp:63-66 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   LetsSplosh in3 — LetsSplosh.cpp:63-66 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   Neuron in0 — Neuron.cpp:62 reads this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SENSE to 0 to 10V
   Neuron in1 — Neuron.cpp:62 reads this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SENSE to 0 to 10V
   Neuron in2 — Neuron.cpp:62 reads this input with getVoltage; NLCShared.hpp:63 clamps the three inputs plus SENSE to 0 to 10V
   Neuron in3 — Neuron.cpp:67-68 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   Neuron in4 — Neuron.cpp:67-68 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   Neuron in5 — Neuron.cpp:67-68 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   Neuron in6 — Neuron.cpp:67-68 read this input with getVoltage; NLCShared.hpp:100-103 clamp the difference to -10V to 10V and split it
   Segue in2 — Segue.cpp:61 reads this input with getVoltage; Segue.cpp:62 clamps the knob plus this input to 0 to 5V
   Splish in0 — Splish.cpp:47-49 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   Splish in1 — Splish.cpp:47-49 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   Splish in2 — Splish.cpp:47-49 read this input with getVoltage; NLCShared.hpp:100-103 clamp each difference to -10V to 10V
   SquidAxon in0 — SquidAxon.cpp:74 sums this input's voltage with the other mixer inputs as a float on each clock edge; SquidAxon.cpp:92 clamps that total to -10V to 10V, and nothing rounds it
   SquidAxon in1 — SquidAxon.cpp:74 sums this input's voltage with the other mixer inputs as a float on each clock edge; SquidAxon.cpp:92 clamps that total to -10V to 10V, and nothing rounds it
   SquidAxon in2 — SquidAxon.cpp:71-82 branch on isConnected and read this input with getVoltage, falling back to OUT 4; SquidAxon.cpp:92 clamps the total

== PdArray
   Array in2 — Array.cpp:350 reads it with a plain getVoltage, rescales it over the POS RANGE span and clamps it to the array
   Miniramp in1 — Miniramp.cpp:220-221 reads it with getPolyVoltage(c) and adds it, scaled by the CV AMT knob, to the knob duration, the total clamped to 0 and 10 seconds

== Prism
   Rainbow in6 — Rainbow.cpp:713 and 716 take the channel count and three channels of getVoltage, halve each and clamp to plus or minus 5, and Tuning.cpp:64-77 raises two to that, so a doubling needs 2V
   Rainbow in7 — Rainbow.cpp:714 and 717 take the channel count and three channels of getVoltage, halve each and clamp to plus or minus 5, and Tuning.cpp:81-94 raises two to that, so a doubling needs 2V
   Rainbow out1 — Rainbow.cpp:760-763 sets twelve channels, writing the envelope clamped to 0 and 10V on the first six and ten times the running level, which reaches 2, on the rest

== RexAudio
   FMtool in0 — FMtool.cpp:64 getPolyVoltage(c) inside a loop bounded by the wider of the carrier and modulator channel counts

== SmarTAZZStudio-Free
   Harmoblender out0 — Harmoblender.cpp Harmoblender::process: setChannels then setVoltage(lvl_Multiplier * the sum of sixteen table reads, idx); each read is scaled to ±5V and multiplied by a level of up to 1, so the sum is not bounded by ±5V, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels
   SuperZzzaw out0 — SuperZzzaw.cpp SuperZzzaw::process: setChannels then setVoltage(left_Out, channel) when both outputs are patched, and setVoltage(left_Out + right_Out, channel) when they are not; each sawtooth reaches ±5V before its level, so twelve of them are not bounded by ±5V, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels

== Stoermelder-P1
   Arena in8 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in9 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in10 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in11 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in12 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in13 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in14 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in15 — Arena.cpp:247 reads it with getVoltage alone and Arena.cpp:250 clamps voltage over 10 to a 0 to 1 x position
   Arena in16 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in17 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in18 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in19 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in20 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in21 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in22 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position
   Arena in23 — Arena.cpp:258 reads it with getVoltage alone and Arena.cpp:261 clamps voltage over 10 to a 0 to 1 y position

== SubmarineFree
   EN-104 in6 — EN1.cpp:85 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in7 — EN1.cpp:85 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in8 — EN1.cpp:85 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in9 — EN1.cpp:85 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in10 — EN1.cpp:87 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in11 — EN1.cpp:87 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in12 — EN1.cpp:87 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in13 — EN1.cpp:87 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in14 — EN1.cpp:89 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in15 — EN1.cpp:89 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in16 — EN1.cpp:89 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in17 — EN1.cpp:89 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in18 — EN1.cpp:90 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in19 — EN1.cpp:90 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in20 — EN1.cpp:90 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in21 — EN1.cpp:90 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in22 — EN1.cpp:92 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in23 — EN1.cpp:92 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in24 — EN1.cpp:92 multiplies this input by 0.1 into a 0 to 1 value, clamped
   EN-104 in25 — EN1.cpp:92 multiplies this input by 0.1 into a 0 to 1 value, clamped
   TF-101 in0 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in1 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in2 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in3 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in4 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in5 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-101 in6 — TF1.cpp:85 multiplies this input by 2 into a text size clamped between 6 and 26
   TF-102 in0 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in1 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in2 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in3 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in4 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in5 — TF1.cpp:78 divides this input by 10 into a 0 to 1 colour component, clamped
   TF-102 in6 — TF1.cpp:85 multiplies this input by 2 into a text size clamped between 6 and 26
   XF-101 in2 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-102 in4 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-102 in5 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-104 in8 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-104 in9 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-104 in10 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-104 in11 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-201 in4 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-202 in8 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-202 in9 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1
   XF-301 in4 — XFX.cpp:125 adds this input to the fade value, which is divided by 10 and clamped to 0 to 1

== TriggerFish-Elements
   TfScenePack4 in0 — TfScenePack4.cpp:43 clamps getChannels to eight and line 49 reads each channel in turn
   TfScenePack4 in1 — TfScenePack4.cpp:43 clamps getChannels to eight and line 49 reads each channel in turn
   TfScenePack4 in2 — TfScenePack4.cpp:43 clamps getChannels to eight and line 49 reads each channel in turn
   TfScenePack4 in3 — TfScenePack4.cpp:43 clamps getChannels to eight and line 49 reads each channel in turn

== VCV-Pro
   Compressor in0 — VCV-Pro 2.2.0 binary: Compressor::process adds up every channel of this port in a loop bounded by its channels byte
   Compressor in1 — VCV-Pro 2.2.0 binary: Compressor::process adds up every channel of this port in a loop bounded by its channels byte
   Compressor in3 — VCV-Pro 2.2.0 binary: Compressor::process adds up every channel of this port in a loop bounded by its channels byte
   Compressor in4 — VCV-Pro 2.2.0 binary: Compressor::process adds up every channel of this port in a loop bounded by its channels byte
   Convolver in6 — VCV-Pro 2.2.0 binary: Convolver::process adds up every channel of this port in a loop bounded by its channels byte
   Convolver in7 — VCV-Pro 2.2.0 binary: Convolver::process adds up every channel of this port in a loop bounded by its channels byte
   Reverb in6 — VCV-Pro 2.2.0 binary: Reverb::process adds up every channel of this port in a loop bounded by its channels byte
   Reverb in7 — VCV-Pro 2.2.0 binary: Reverb::process adds up every channel of this port in a loop bounded by its channels byte

== ZZC
   SRC in0 — SRC.cpp:161 clamp(coarse + fine + getVoltage(), -11.0f, 11.0f)

== computerscare
   computerscare-horse-a-doodle-doo in3 — ComputerscareHorseADoodleDoo.cpp:549 takes getChannels() on this input and ComputerscareHorseADoodleDoo.cpp:594 floors the trimmed input plus the knob into a step count, clamped to 2 or more at ComputerscareHorseADoodleDoo.cpp:608
   computerscare-horse-a-doodle-doo in4 — ComputerscareHorseADoodleDoo.cpp:550 takes getChannels() on this input and ComputerscareHorseADoodleDoo.cpp:598 divides it by 10, multiplies by the trim knob and adds the density knob, and ComputerscareHorseADoodleDoo.cpp:609 clamps the total to 0..1
   computerscare-portaloof in8 — PortaloofModule.hpp:670 takes getChannels() on this input, one channel per source, PortaloofModule.hpp:682 multiplies it by the attenuverter and the row's CV scale of 0.5, and PortaloofModule.hpp:685 clamps the knob-plus-input total to -5..5, the span half of ±10V covers
   computerscare-portaloof in9 — PortaloofModule.hpp:670 takes getChannels() on this input, one channel per source, PortaloofModule.hpp:682 multiplies it by the attenuverter and the row's CV scale of 0.5, and PortaloofModule.hpp:685 clamps the knob-plus-input total to -5..5, the span half of ±10V covers
   computerscare-portaloof in21 — PortaloofModule.hpp:659 divides this input by 5 and PortaloofModule.hpp:661 clamps the knob-plus-input total to -1..1, the span a fifth of ±5V covers

== dbRackModules
   AP in1 — AP.cpp:92 reads it four channels at a time, scales it by 0.05 and the Freq CV knob, adds the Freq knob and clamps the total to 0.001..0.49
   AP in2 — AP.cpp:87 reads it with getVoltage() and no channel loop, scales it by 0.001 and the Delay CV knob, adds the Delay knob and clamps the total to 0..0.5 seconds
   CLP in2 — CLP.cpp:42 reads it four channels at a time, scales it by the Drive CV knob, adds the Drv knob and clamps the total to 0..10
   CSOSC in2 — CSOSC.cpp:104-106 read it four channels at a time, scale it by a tenth and the Skew Amt knob, add the Skew knob and clamp the total to 0..1
   CSOSC in3 — CSOSC.cpp:107-109 read it four channels at a time, scale it by a tenth and the Clip Amt knob, add the Clip knob and clamp the total to 0..1
   DRM in2 — DRM.cpp:104 reads it four channels at a time, scales it by the Offset CV knob, adds the Offset knob to the carrier and clamps the total to -5..5
   DRM in3 — DRM.cpp:75 reads it four channels at a time, scales it by the VB CV knob, adds the Vb knob and clamps the total to just above zero and 5
   DRM in4 — DRM.cpp:76 reads it four channels at a time, scales it by the VL-VB CV knob, adds the Vl-Vb knob and clamps the total to just above zero and 5
   DRM in5 — DRM.cpp:77 reads it four channels at a time, scales it by the Gain CV knob, adds the Slope knob and clamps the total to 0..1
   Drums in1 — Drums.cpp:254 reads it with getVoltage() and no channel loop, scales it by a tenth and the Decay CV knob, adds the Dec knob and Drums.cpp:255 clamps the total to 0..1
   Faders in0 — Faders.cpp:236 reads channel c of this input, scales it by the Mod CV Amount knob, adds the fader and clamps the total to that bank's span
   Faders in1 — Faders.cpp:236 reads channel c of this input, scales it by the Mod CV Amount knob, adds the fader and clamps the total to that bank's span
   Faders in2 — Faders.cpp:236 reads channel c of this input, scales it by the Mod CV Amount knob, adds the fader and clamps the total to that bank's span
   FadersOne in0 — Faders.cpp:658 reads channel c of this input, scales it by the Mod CV Amount knob, adds the fader and clamps the total to the span
   GeneticSuperTerrain in7 — GeneticTerrain.cpp reads it four channels at a time, scales it by the knob beside it, adds the N1 knob and clamps the total
   GeneticSuperTerrain in8 — GeneticTerrain.cpp reads it four channels at a time, scales it by the knob beside it, adds the N2 knob and clamps the total to -5..5
   GeneticSuperTerrain in9 — GeneticTerrain.cpp reads it four channels at a time, scales it by the knob beside it, adds the N3 knob and clamps the total to -5..5
   GeneticSuperTerrain in10 — GeneticTerrain.cpp reads it four channels at a time, scales it by the knob beside it, adds the A knob and clamps the total to 0.05..5
   GeneticSuperTerrain in11 — GeneticTerrain.cpp reads it four channels at a time, scales it by the knob beside it, adds the B knob and clamps the total to 0.05..5
   JWS in2 — WS.cpp:63 reads it four channels at a time, scales it by the A CV knob, adds the A knob and clamps the total to 0..5
   JWS in3 — WS.cpp:64 reads it four channels at a time, scales it by the B CV knob, adds the B knob and clamps the total to 0..5
   JWS in4 — WS.cpp:65 reads it four channels at a time, scales it by the C CV knob, adds the C knob and clamps the total to 0..5
   JWS in5 — WS.cpp:66 reads it four channels at a time, scales it by the D CV knob, adds the D knob and clamps the total to 0..5
   L4P in3 — L4P.cpp:78 reads it four channels at a time, scales it by a tenth and the R CV knob, adds the R knob and clamps the total to 0..1
   LWF in1 — LWF.cpp:199 reads it four channels at a time, scales it by a tenth and the Input gain CV knob, adds the In-Gain knob and clamps the total to 0..1
   LWF in2 — LWF.cpp:200 reads it four channels at a time, scales it by the Offset CV knob, adds the Offset knob and clamps the total to -5..5
   LWF in3 — LWF.cpp:201 reads it four channels at a time, scales it by 5000 and the Resistor CV knob, adds the Resistor knob and clamps the total to 10000..100000 ohms
   LWF in4 — LWF.cpp:202 reads it four channels at a time, scales it by 500 and the Load resistor CV knob, adds the Load Resistor knob and clamps the total to 1000..10000 ohms
   LWF in5 — LWF.cpp:203 reads it four channels at a time, scales it by a tenth and the Output gain CV knob, adds the Out-Gain knob and clamps the total to 0..1
   Osc5 in2 — Osc5.cpp:111 reads it four channels at a time, scales it by the first breakpoint CV knob, adds the knob and clamps the total
   Osc5 in3 — Osc5.cpp:116 reads it four channels at a time, scales it by the second breakpoint CV knob, adds the knob and clamps the total
   Osc7 in2 — Osc7.cpp:119-121 read it four channels at a time, divide by 10, scale it by the Depth CV knob, add the Depth knob and clamp the total
   Osc8 in2 — Osc8.cpp:249 reads it four channels at a time, scales it by the Decay CV knob, adds the Decay knob and clamps the total to 0.1..3
   Osc8 in3 — Osc8.cpp reads it four channels at a time, scales it by the Decay CV knob, adds the Decay knob and clamps the total to 0.1..3
   Osc8 in4 — Osc8.cpp:251 reads it four channels at a time, scales it by a tenth and the Odd/Even CV knob, adds the knob and clamps the total to -1..1
   Osc8 in5 — Osc8.cpp reads it four channels at a time, scales it by a tenth and the Odd/Even CV knob, adds the knob and clamps the total to -1..1
   Osc8 in6 — Osc8.cpp reads it four channels at a time, scales it by the Stretch CV knob, adds the Stretch knob and clamps the total
   Osc8 in7 — Osc8.cpp reads it four channels at a time, scales it by the Stretch CV knob, adds the Stretch knob and clamps the total
   Osc9 in2 — Osc9.cpp:137 reads it four channels at a time, scales it by a tenth and the P1 CV knob, adds the P1 knob and clamps the total to 0..1
   Osc9 in3 — Osc9.cpp:139 reads it four channels at a time, scales it by the P2 CV knob, adds the P2 knob and clamps the total to 0..5
   OscA1 in2 — OscA1.cpp:383 reads it four channels at a time, scales it by the Decay CV knob, adds the Decay knob and clamps the total to 0.1..3
   OscA1 in3 — OscA1.cpp:385 reads it four channels at a time, scales it by a tenth and the Odd/Even CV knob, adds the knob and clamps the total to -1..1
   OscA1 in4 — OscA1.cpp:387 reads it four channels at a time, scales it by a tenth and the Stretch CV knob, adds the knob and clamps the total
   OscA1 in8 — OscA1.cpp:339 reads it with getVoltage() and no channel loop, scales it by a tenth and the Comb Amp CV knob, adds the knob and clamps the total
   OscA1 in9 — OscA1.cpp:345 reads it with getVoltage() and no channel loop, scales it by half and the Comb Peek CV knob, adds the knob and clamps the total to 0.1..4
   OscA1 in10 — OscA1.cpp:341 reads it with getVoltage() and no channel loop, scales it by ten and the Comb Freq CV knob, adds the knob and clamps the total to 1..100
   OscA1 in11 — OscA1.cpp:348 reads it with getVoltage() and no channel loop, scales it by a tenth and the Comb Warp CV knob, adds the knob and clamps the total to 0.2..1
   OscA1 in12 — OscA1.cpp:343 reads it with getVoltage() and no channel loop, scales it by the Comb Phs CV knob, adds the knob and clamps the total to one turn
   OscP in1 — OscP.cpp:206 reads it four channels at a time, scales it by a tenth and the width CV knob, adds the width knob and clamps the total
   Pad2 in1 — Pad2.cpp:186-188 read it with getVoltage() and no channel loop, scale it by three and the Bandwidth CV knob, add the BW knob and clamp the total to 0.5..60
   Pad2 in2 — Pad2.cpp:191-193 read it with getVoltage() and no channel loop, scale it by a fifth and the Bandwidth Scale CV knob, add the Scl knob and clamp the total to 0.5..4
   Pulsar in1 — Pulsar.cpp:148 reads it four channels at a time, scales it by a fifth and the D CV knob, adds the Duty knob and clamps the total to 0.01..1.99
   Pulsar in2 — Pulsar.cpp:152 reads it four channels at a time, scales it by three tenths and the Waveform CV knob, adds the Waveform knob and clamps the total to 0..3
   RndH2 in4 — RndH2.cpp:127 reads it per channel with getPolyVoltage(chn), scales it by 0.1 and the Range CV knob, adds the Range knob and clamps the total to 0..1
   RndH2 in5 — RndH2.cpp:128 reads it per channel with getPolyVoltage(chn), scales it by the Ofs CV knob, adds the Ofs knob and clamps the total to -5..5
   SKF in3 — SKF.cpp:82 reads it four channels at a time, scales it by a tenth and the R CV knob, adds the R knob and clamps the total to 0..1
   SPF in3 — SPF.cpp:80 reads it four channels at a time, scales it by a fifth and the R CV knob, adds the R knob and clamps the total to 0..1.999 before taking it from 2
   SWF in2 — SWF.cpp:50 takes the size of this jack with fabs, scales it by the Gain CV knob, adds the Gain knob and clamps the total to -10..10
   SWF in3 — SWF.cpp:51 reads it four channels at a time, scales it by half the Symmetry CV knob, adds the Symm knob and clamps the total to -5..5
   SuperLFO in6 — SuperLFO.cpp:190-191 read it per channel, scale it by the knob beside it, add the N1 knob and clamp the total to 0.05..16
   SuperLFO in7 — SuperLFO.cpp:200-201 read it per channel, scale it by the knob beside it, add the N2 knob and clamp the total to -5..5
   SuperLFO in8 — SuperLFO.cpp:202-203 read it per channel, scale it by the knob beside it, add the N3 knob and clamp the total to -5..5
   SuperLFO in9 — SuperLFO.cpp:194-195 read it per channel, scale it by the knob beside it, add the A knob and clamp the total to 0.05..5
   SuperLFO in10 — SuperLFO.cpp:197-198 read it per channel, scale it by the knob beside it, add the B knob and clamp the total to 0.05..5
   USVF in2 — USVF.cpp:118 reads it four channels at a time, scales it by a tenth and the Q CV knob, adds the Q knob and clamps the total to 0.5..20
   USVF in3 — USVF.cpp:119 reads it four channels at a time, scales it by a tenth and the Drive CV knob, adds the Drv knob and clamps the total to 0..1

== forsitan
   aether out0 — src/aether.cpp:131, the recovered signal crossfaded with the dry one, with no stated clamp
   antrum out0 — src/antrum.cpp:169, one setVoltage per side, with no stated clamp
   caligo out0 — src/caligo.cpp:214, the loop tapped after the long delay, with no stated clamp
   caligo out2 — src/caligo.cpp:216, one setVoltage per side after the mix, with no stated clamp
   cartilago out0 — src/cartilago.cpp:394, setChannels from the audio input, with no stated clamp
   guttur out1 — src/guttur.cpp:425, the chaotic state after the distortion, clamped and DC blocked
   raucus out0 — src/raucus.cpp:169, setChannels from the audio input, with no stated clamp
   rete out0 — src/rete.cpp:198, the mixed left side times a compensation factor, with no stated clamp
   sylla out0 — src/sylla.cpp:524, one setVoltage of the played sample, with no stated clamp
   textor out0 — src/textor.cpp:822, one setVoltage per side, with no stated clamp
   vespae out0 — src/vespae.cpp:314, one of the five simultaneous filter nodes, with no stated clamp
   vestigia out0 — src/vestigia.cpp:321, one setVoltage per side, with no stated clamp
   viginti out0 — src/viginti.cpp:86, one channel out per channel in, with no stated clamp
   vorax out0 — src/vorax.cpp:663, one setVoltage per side after the loop and echo, with no stated clamp
   vorax out1 — src/vorax.cpp:664, one setVoltage per side after the loop and echo, with no stated clamp

== gwrthiant
   graddfa in0 — graddfa.cpp:45-48 reads getVoltage() with no channel; line 51 clamps the result to a floor of 0, so any negative note voltage gives the same output as C4
   nilyniant in0 — nilyniant.cpp:49-52 reads getVoltage() with no channel; lines 55 and 59 clamp both results to a floor of 0, so any negative note voltage gives the same output as C4

== noobhour
   baseliner in0 — src/Baseliner.cpp process(): channels[i] = inputs[SIGNAL1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in1 — src/Baseliner.cpp process(): channels[i] = inputs[SIGNAL1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in2 — src/Baseliner.cpp process(): channels[i] = inputs[SIGNAL1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in3 — src/Baseliner.cpp process(): channels[i] = inputs[SIGNAL1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in4 — src/Baseliner.cpp process(): channels[i] = inputs[BASE1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in5 — src/Baseliner.cpp process(): channels[i] = inputs[BASE1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in6 — src/Baseliner.cpp process(): channels[i] = inputs[BASE1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   baseliner in7 — src/Baseliner.cpp process(): channels[i] = inputs[BASE1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   bsl1r in0 — src/Baseliner.cpp process(): channels[i] = inputs[SIGNAL1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)
   bsl1r in4 — src/Baseliner.cpp process(): channels[i] = inputs[BASE1_INPUT + i].getChannels() and the loop reads getVoltage(c), then clamp(input*param + absVal, -10.f, 10.f)

== wintoid
   Brink in2 — src/Brink/Brink.cpp:247 multiplies it by the attenuverter and adds it to the WIDTH knob; src/Brink/dsp.h:142 clamps the span to MIN_WIDTH 0.001 and MAX_WIDTH 20
   Brink in5 — src/Brink/Brink.cpp:207 substitutes the A_WIDTH_CV_INPUT port object when this jack is not connected; src/Brink/Brink.cpp:247 then scales and adds it, and src/Brink/dsp.h:142 clamps the span to 0.001..20
   BrinkV2 in2 — src/BrinkV2/BrinkV2.cpp:245 multiplies it by the attenuverter and adds it to the WIDTH knob; src/Brink/dsp.h:142 clamps the span to MIN_WIDTH 0.001 and MAX_WIDTH 20
   BrinkV2 in5 — src/BrinkV2/BrinkV2.cpp:205 substitutes the A_WIDTH_CV_INPUT port object when this jack is not connected; src/BrinkV2/BrinkV2.cpp:245 then scales and adds it, and src/Brink/dsp.h:142 clamps the span to 0.001..20
   VortexMM in2 — src/Vortex/Vortex.cpp:167 scales it by the attenuverter and 0.2, subtracts it from the damping and clamps the damping to 0.01..0.707
   VortexV2 in2 — src/VortexV2/VortexV2.cpp:238 scales it by the attenuverter and 0.2, subtracts it from the damping and clamps the damping to 0.01..0.707

