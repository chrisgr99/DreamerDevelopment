# Rendered panels, 5.6 MB of them. Regenerate with annotate.py or rsvg-convert from the
# plugins already installed; there is nothing here that is not derived.
panels/

# The per-maker census slices, 5 MB of them. Derived from the census the Dark module writes;
# regenerate with make_census_slices.py.
help/census/
